version: '3.8'
services:
  mongo:
    image: mongo:6
    container_name: mongo_poc
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: secret
    ports:
      - "27017:27017"
    volumes:
      - ./mongo-data:/data/db
